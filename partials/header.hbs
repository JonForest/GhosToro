{{^is "home"}}
	<header id=header class="main-header post-head {{#if image}}" style="background-image: url({{image}}){{else}}no-cover{{/if}}">
{{else}}
	<header id=header>
{{/is}}
	{{#if @blog.logo}}
		<a class="image-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}} Logo" /></a>
	{{else}}
	    <a class="logo" href="/">
	        reaching <span>for space</span>
	    </a>
    {{/if}}
    {{^is 'home'}}
    	{{> menu}}
    {{/is}}
</header>
